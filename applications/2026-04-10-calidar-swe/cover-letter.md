Hi Calidar team,

Three of my four major projects are signal processing pipelines. PianoTranscriber trains a PyTorch CNN on spectrograms at 16 kHz / 11 ms resolution, converting 100GB of piano audio into MIDI. SonicGen does spectral peak detection and constellation hashing to fingerprint audio, 14,000 lines of Python written by hand. Voices That Remain preprocesses document images and runs them through a multi-stage AI vision pipeline that extracts structured data from handwritten text. The common thread is taking raw signal data, building a reliable multi-stage processing pipeline around it, and producing structured, validated output.

What drew me to Calidar: you're a small team building 4D mammography, which means the software has to be as precise as the imaging hardware. At that scale, one engineer touches everything from data pipelines to interfaces to deployment. That's how I work. VTR is a 58,700-line production app where I built the React frontend, Express backend, 20-table Postgres schema, GCP deployment pipeline, and the multi-stage AI processing system, all solo. I don't need a large team to ship production software.

I deliberately wrote SonicGen by hand through 2025, no coding agents, because I wanted to understand code at the level where I can own every line. I switched to agent-directed development in 2026 for VTR. The transition was easy because I already understood what the agents were producing. For a medical device company where correctness matters more than speed, that foundation of understanding is the part that counts.

I'm in Raleigh, short drive from RTP. I'd like to learn more about the software side of what you're building.

Best,

Mason Galusha
masonlgalusha.careers@gmail.com
304-546-5850
github.com/MLGalusha
linkedin.com/in/masonlgalusha
